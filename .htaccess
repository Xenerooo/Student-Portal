<IfModule mod_rewrite.c>
    RewriteEngine On

    # Stop processing if we're already in the public directory (inf loop prevention)
    RewriteRule ^public/ - [L]

    # Rewrite all requests to the public directory
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
